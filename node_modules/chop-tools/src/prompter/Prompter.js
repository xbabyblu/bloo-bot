const PromptFilter = require('../structures/PromptFilter');

class Prompter {
  static async message(options) {
    // do stuff
  }

  static async reaction(options) {
    // do stuff
  }

  static async vote(options) {
    // do stuff
  }

  static async choice(options) {
    // do stuff
  }
}

module.exports = Prompter;
